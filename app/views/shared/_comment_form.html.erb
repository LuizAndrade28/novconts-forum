<%= simple_form_for [@post, @comment] do |f| %>
  <%= f.error_notification %>
  <% if f.object.errors[:base].present? %>
    <%= f.error_notification message: f.object.errors[:base].to_sentence %>
  <% end %>
  <div class="form-inputs">
    <%= f.input :content, label: 'Conteúdo', as: :text, input_html: { rows: 10, maxlength: "245" } %>
  </div>

  <div class="form-actions d-flex pt-4">
    <%= f.submit "Salvar", class: "flex-grow-1 w-100 btn btn-outline-primary shadow-sm me-3" %>
    <%= link_to 'Voltar', root_path, class: "flex-grow-1 w-100 btn btn-outline-primary shadow-sm" %>
  </div>
<% end %>
